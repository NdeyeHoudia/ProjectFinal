Comment utiliser le shell ssh / mongo

docker exec -it mongodb bash

Démarrer les services dans un fichier docker-compose.yml

docker-compose up -d

Arrêter et supprimer les services et leurs volumes

docker-compose down -v
